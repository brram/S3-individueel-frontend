import { Menudishmenuingredient } from './menudishmenuingredient';

describe('Menudishmenuingredient', () => {
  it('should create an instance', () => {
    expect(new Menudishmenuingredient()).toBeTruthy();
  });
});
